export const useContentNavigator = ({ ...courseContents }) => {
  const currentTopicIndex = ref(0)
  const currentContentIndex = ref(0)

  const nextContent = () => {}

  const previousContent = () => {}
}
